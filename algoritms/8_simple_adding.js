export const simpleAdding = (num) => {
  let result = 0;
  for (let i = 0; i <= num; i++) {
    result += i;
  }
  return result;
};

/*
Verilecek olan tamsayiya kadar olan degerlerin toplanmasi ( verilen sayi dahi)

*/
